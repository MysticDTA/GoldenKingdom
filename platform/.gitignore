# 🧬 Dependencies
node_modules/

# 🔐 Lockfiles (keep if you want reproducible builds)
# package-lock.json
# yarn.lock
# pnpm-lock.yaml

# ⚙️ Build outputs
.next/
out/
dist/

# 🌫️ Environment files (never commit secrets)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# 📜 Logs
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# 🖼️ OS/Editor junk
.DS_Store
*.swp
*.swo

# ✅ Scripts — keep custom scripts tracked
# (don’t ignore scripts/, since we want check-env.js etc.)
# If you want to ignore temporary files only:
scripts/*.log
scripts/*.tmp
